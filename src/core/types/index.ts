export enum PresentationSlideTypeEnum {
    MULTIPLE_CHOICE = "multiple_choice",
    HEADING = "heading",
    PARAGRAPH = "paragraph",
}

export enum PresentationPaceStateEnum {
    IDLE = "idle",
    PRESENTING = "presenting",
}

export enum PresentPresentationActionEnum {
    PRESENT = "present",
    CHANGE_SLIDE = "change_slide",
    QUIT = "quit",
}

export enum VotingAnswerTypeEnum {
    CHOICES = "choices",
    REACTION = "reaction",
    // RANKING = "ranking",
}
